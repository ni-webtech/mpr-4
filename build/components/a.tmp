    name="mpr"
    path="$1"
    search="src/deps:src"

    path=`probe --path "$path" --partial "mpr" $name`
    if [ "$path" = "" ] ; then
        warnComponent $name
        return
    fi
    if [ "$path" = "${path#src/}" ] ; then
        libpaths="$path/lib"
    fi
