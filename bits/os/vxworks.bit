/*
    vxworks.bit -- VxWorks Settings
 */

Bit.load({
    suffixes: {
        lib:   'a',
        shlib: 'out',
        shobj: 'out',
        obj:   'o',
        exe:   'out',
    },

    '+required': [ 'compiler' ],
    '+optional': [ ],
})
