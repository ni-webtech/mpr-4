/*
    vxworks.bit -- VxWorks Settings
 */

bit({
    extensions: {
        lib:   'a',
        shlib: 'out',
        shobj: 'out',
        obj:   'o',
        exe:   'out',
    },

    '+required': [ 'compiler' ],
    '+optional': [ ],
})
